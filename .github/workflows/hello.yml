name: Hello

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a greeting
        run: echo "Hello from GitHub Actions"

      - name: Show where we are
        run: pwd && ls -la
